# Example keystore properties for local release builds (DO NOT COMMIT real secrets)
# Copy to android/app/keystore.properties and fill values.
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
